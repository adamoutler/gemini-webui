I will consult the `ux-architect` sub-agent to draft a comprehensive plan for adding the requested friction and "undefinitive proof" markers to direct fake gemini usage.
I apologize for the internal tool error. I will step into the role of the UX Architect and outline the implementation strategy directly.

Here is the architectural plan to increase friction, prevent test pollution, and invalidate direct invocations of `fake_gemini.py`:

### 1. Backend Verification & Console Friction
**Objective:** Make direct CLI usage of `fake_gemini.py` annoying and visibly flawed.
*   **Harness Injection:** The new ephemeral session route must generate a one-time UUID and pass it as an environment variable (e.g., `GEMINI_WEBUI_HARNESS_ID=<uuid>`) when executing `pty.fork()`.
*   **Startup Friction:** Modify `src/fake_gemini.py` to check for this environment variable. If it is missing:
    *   **Time Penalty:** Introduce an artificial 5-to-10 second `time.sleep()` delay on startup.
    *   **Console Warning:** Print a highly visible, colored stderr banner:
        ```text
        ======================================================================
        [WARNING] UNDEFINITIVE PROOF: BYPASSING TEST HARNESS
        fake_gemini.py was invoked directly without the WebUI Session Harness.
        Test results using this output will be rejected by the QA Gate.
        ======================================================================
        ```
    *   **Payload Corruption:** Prepend or append the string `[UNDEFINITIVE PROOF - BYPASSED HARNESS]` to *every* simulated AI response chunk. This will intentionally break expected formatting and pollute the terminal stream.

### 2. UX & Visual Markers
**Objective:** Ensure that visual evidence (screenshots) unmistakably reveals the context of the test.
*   **Valid Harness Mode:** When the web interface detects it is running a valid ephemeral fake session, it should apply a persistent, unmistakable CSS overlay to the xterm.js container (e.g., a striped black-and-yellow hazard border and a `⚠️ HARNESS TEST MODE ⚠️` watermark in the corner).
*   **Bypassed Mode (Undefinitive):** Because of the "Payload Corruption" step above, any screenshot taken of `fake_gemini.py` running in a standard terminal (or without the web UI overlay) will be flooded with `[UNDEFINITIVE PROOF]` text, making the visual evidence obviously flawed and unusable.

### 3. QA Pipeline Integration (The "Machine" Gate)
**Objective:** Programmatically reject commits that attempt to use bypassed test evidence.
*   **Log & Artifact Scanning:** Update `.gemini/hooks/qa-gate.sh` (or the corresponding CI/CD test runner) to explicitly search for the string `"UNDEFINITIVE PROOF"` within all test output logs, `stdout/stderr` captures, and saved test artifacts in `/tmp`.
*   **Hard Rejection:** If the string is detected anywhere in the evidence payload, the QA Gate must automatically fail the commit with the message:
    > *"REJECTED: Test pollution detected. Evidence contains undefinitive proof from a direct invocation of fake_gemini.py. You must use the WebUI ephemeral session harness for definitive testing."*

This combination of runtime delay, UI watermarking, payload pollution, and automated CI rejection provides the necessary friction to force adherence to the new testing paradigm.
