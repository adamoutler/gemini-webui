[pytest]
addopts = --timeout=60 -p no:anyio
timeout_func_only = false
timeout_method = signal
markers =
    prone_to_timeout: mark a test as prone to hanging, requiring strict timeout enforcement
    timeout: mark a test to timeout after a given number of seconds
filterwarnings =
    ignore:tagMap is deprecated
    ignore:typeMap is deprecated
    ignore:.*The `ordered` `class Meta` option is deprecated.*
    ignore:.*use of forkpty.*
